{
  "name": "hermes-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "test": "npm run test:run --prefix backend && npm run test:run --prefix frontend",
    "aws:enable": "npm run aws:enable --prefix backend",
    "aws:disable": "npm run aws:disable --prefix backend",
    "aws:status": "npm run aws:status --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
